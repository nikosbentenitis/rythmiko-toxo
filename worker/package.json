{
  "name": "rhythmiko-auth",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that exchanges GitHub OAuth codes for tokens on behalf of the rhythm validator.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
